apiVersion: v2
name: datamigratie
description: A Helm chart for the Datamigratie Component

type: application

# Chart version and appVersion will be filled in during a release, leave as is
version: 0.0.0
appVersion: 0.0.0

dependencies:
  - name: postgresql
    version: 12.5.6
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
