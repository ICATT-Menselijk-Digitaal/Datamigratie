apiVersion: v1
kind: Service
metadata:
  name: {{ include "datamigratie.fullname" . }}
  labels:
    {{- include "datamigratie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "datamigratie.selectorLabels" . | nindent 4 }}
